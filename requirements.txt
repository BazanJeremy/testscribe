# TestScribe — pinned dependencies
# Generated for Python 3.12

# Core
pydantic>=2.7,<3.0
anthropic>=0.28,<1.0
flask>=3.0,<4.0
python-dotenv>=1.0

# AI / Vector store
chromadb>=0.5,<2.0
scikit-learn>=1.5,<2.0
numpy>=1.26,<3.0

# Optional — sentence-transformers (production neural embeddings)
# Requires ~500MB model download; TF-IDF fallback used in CI
# sentence-transformers>=3.0,<4.0

# Templating
jinja2>=3.1

# Dev / test
pytest>=8.2
pytest-cov>=5.0
hypothesis>=6.100
ruff>=0.4
mypy>=1.10
allure-pytest>=2.13

# E2E (Week 3)
# playwright>=1.44
# pytest-playwright>=0.5
