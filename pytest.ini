[pytest]
testpaths = tests
addopts = --tb=short -q
pythonpath = src
markers =
    unit: isolated unit tests
    integration: integration tests requiring pipeline
    e2e: end-to-end Playwright tests
    live_api: tests requiring Anthropic API key (skipped in CI)
