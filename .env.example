# TestScribe environment configuration
# Copy to .env and fill in values

# Optional — agents use rule-based fallback if not set
ANTHROPIC_API_KEY=

# Flask
FLASK_ENV=development
FLASK_PORT=5000

# ChromaDB (Week 2)
CHROMA_PERSIST_PATH=./data/chroma_db
